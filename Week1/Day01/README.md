 Day 1 - SQL Basics & Aggregation (Up to GROUP BY) Objective

Build a strong foundation in SQL by learning core querying concepts, filtering data, performing aggregations, and grouping results for analysis.

Files in This Folder No structured files created yet Practice queries were executed directly (e.g., DB Fiddle / SQL environment) Dataset Overview

The dataset used consists of 3 tables:

Employee → employee details (id, name, age, salary, department, hire_date) Department → department information Project → project details linked to departments Concepts Covered

Basic Queries SELECT all columns and specific columns Filtering using WHERE Conditions like >, <, =
String Matching Pattern matching using LIKE Wildcards: % → any number of characters _ → single character
Date Queries Filtering using date conditions Comparing dates Extracting specific time-based data
Aggregate Functions SUM() → total values AVG() → average MAX() / MIN() → highest/lowest COUNT() → number of records
GROUP BY Operations Grouping data based on columns Applying aggregate functions on grouped data Understanding how grouped queries work Practice Questions Solved (1–25) Basic Queries (1–5) Retrieve all records Select specific columns Filter based on conditions String Matching (6–10) Pattern-based filtering using LIKE Working with wildcards Date Queries (11–15) Filtering based on dates Comparing hire dates Aggregate Queries (16–20) Total, average, and maximum values Counting records Department-wise aggregations GROUP BY Queries (21–25) Group-based calculations Department-wise analysis Identifying highest values per group Key Concepts Demonstrated Writing SQL queries from scratch Filtering and pattern matching Using aggregate functions Grouping data using GROUP BY Outcome
Day 1 establishes a strong base in SQL fundamentals, especially aggregation and grouping, which are essential for data analysis and interviews.

Notes for Review Practice GROUP BY + aggregate functions (very important) Focus on writing queries without help Next step: HAVING clause and JOINS
